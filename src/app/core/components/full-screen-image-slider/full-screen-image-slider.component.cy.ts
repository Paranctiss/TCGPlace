import { FullScreenImageSliderComponent } from './full-screen-image-slider.component'

describe('FullScreenImageSliderComponent', () => {
  it('should mount', () => {
    cy.mount(FullScreenImageSliderComponent)
  })
})