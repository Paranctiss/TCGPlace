import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.projetcsc.tcgplace',
  appName: 'TCGPlace',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
